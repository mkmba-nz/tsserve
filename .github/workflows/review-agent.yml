name: Review Agent

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]

permissions:
  pull-requests: write
  id-token: write
  contents: read

jobs:
  review:
    uses: mkmba-nz/github-infra/.github/workflows/review-agent.yml@main
    secrets: inherit
